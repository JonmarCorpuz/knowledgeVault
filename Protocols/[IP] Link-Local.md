# Link-Local IP Address Overview

A link-local address is an IP address that's only valid and usable on the single local network segment where it's assigned

| Link-Local IP Range | IP Version |
| --- | --- |
| `169.254.0.0/16` | IPv4 |
| `fe80::/10` | IPv6 |