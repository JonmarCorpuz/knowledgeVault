# API Service Endpoint Overview

An API service endpoint is the specific digital location where an API receives requests for data or functionality

* Allows a client to access or manipulate a particular resource or service by acting as the gateway for communication between a client and the server
* Each endpoint corresponds to a unique URL