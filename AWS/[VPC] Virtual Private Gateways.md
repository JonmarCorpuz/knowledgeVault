# Virtual Private Gateway Overview

VGWs are AWS-side VPN endpoints that users can attach to a VPC to terminate Site-to-Site VPN or Direct Connect connections from on-prem or other networks

* Lets users reach VPC subnets using private IPs over encrypted tunnels 
* Logically placed at the edge of the VPC it's attached to