# Relational Database Service Overview

RDS is a fully managed cloud database service that simplifies setting up, operating, and scaling relational databases

* Handles routine tasks (Ex: *Backups*, *Patching*, *Recovery*)