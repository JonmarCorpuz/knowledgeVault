# Database Subnet Group Overview

A database subnet group is a collection of subnets within a VPC that specifies where the database instance can be placed across Availability Zones for high availability and fault tolerance

* Helps ensure that an RDS instance deploys in at least two subnets in different AZs