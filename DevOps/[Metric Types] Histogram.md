# Histogram Metrics Overview

A histogram metric measures the distribution of values across predefined buckets where each bucket acts as a cumulative counter that tracks observations below or equal to a threshold

* Focuses on shape and percentiles via queries

<br>

# Histogram Visualization

## Heatmaps

A heatmap is a graph that displays metric density, patterns, and anomalies across dimensions with color intensity that represents value magnitude