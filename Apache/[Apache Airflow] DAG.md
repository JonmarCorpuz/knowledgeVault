# Apache Airflow DAG Overview

DAGs in Apache Airflow on GCP is a collection of tasks organized with dependencies that define the sequence and workflow of a data pipeline or a series of processing operations

* Mainly defined using Python code in order to specify task details and their relationships, scheduling rules, and execution parameters